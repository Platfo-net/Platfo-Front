import { ITextArea } from '@/components/dataEntry/TextArea/TextArea';

const base: ITextArea = {
  size: 'md',
  variant: 'outlined',
  color: 'default',
  label: 'Enter Name',
  placeholder: 'Your name',
};

export const mockITextAreaProps = {
  base,
};
